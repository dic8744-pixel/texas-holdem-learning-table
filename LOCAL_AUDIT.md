# 本地学习版审计与验收记录

日期：2026-08-23

## 上游与许可证

- 上游公开仓库 `best-trading-indicator-tools/poker` 已通过 Git 完整获取并保留提交历史。
- 完整 Git 工作树位于当前目录；公开发布时上游地址保留为 `upstream`，新仓库使用 `origin`。
- 审计基线提交：`42f4d675e180f284388db6d7e9de4cb90c7d3c77`。
- 上游和本地修改均保留 `LICENSE` 中的 AGPL-3.0 条款。
- `THIRD_PARTY_NOTICES.md` 保留 b-inary postflop-solver / wasm-postflop 的 AGPL 署名和锁定提交，以及 Comlink 4.4.1 的 Apache-2.0 署名与许可证。
- 仅在本机私人运行不等于向外分发。如果将修改版分发，或通过网络向其他用户提供交互，需继续履行 AGPL-3.0 的对应源码和许可证告知义务。

## 上游基线实测

在修改前实际执行了上游的规则、范围、翻牌前策略包/信任边界/运行合法性、求解路由、牌桌场景和严格教练审计。基线逻辑测试通过；原始布局测试因没有预装 Playwright 浏览器而不能直接运行，本地版已改为使用 Mac 上安装的 Google Chrome 验收。

## 审计发现与修正

- 原引擎没有烧牌；现在翻牌、转牌、河牌前各烧一张，并进入 52 张牌守恒检查。
- 原引擎只判断单次短码全押是否达到完整加注；现在会按玩家上次行动时的注码水平计算“累计短码全押”是否重新开放加注。
- 增加 `legalActions` 合法行动契约，并让引擎直接识别 Fold / Check / Call / Bet / Raise / All-in 六种语义行动。AI 的最终决策仍经由同一规则引擎应用。
- 摊牌不再只保存牌型分数；现在同时保存、显示并导出精确的最佳五张牌。
- 现金桌改为固定 50/100 积分、1～5 份买入、玩家与 AI 可不同深度。玩家不再破产自动买入，补充按钮只在手局结束后有效。
- 玩家补充次数与 AI 自动重新买入次数分开；现金桌即时净值包含已投入当前街的筹码。
- 新增默认中文界面、中文牌型/行动/摊牌/结果/教练文字，并把启发式与近似求解器的边界明确告知用户。
- 增加 3-bet、WTSD、W$SD 和净积分统计，以及设置、不同买入深度、回放和学习记录的本地保存。

## 隐藏信息边界

AI 胜率模拟使用自己的底牌、已公开的公共牌、可见行动和估计范围重新构造未知牌集。AI 决策路径不读取 `state.deck` 或其他玩家的 `hole`。高难度增加模拟次数、范围后验、尺度、阻断牌和可见行动倾向模型，不使用偷看提升强度。

## 2026-08-23 测试结果

可重复命令：`./test-local.command`

- 规则回归：通过。覆盖可重复洗牌、最佳五张、边池、平分零头、短码全押重开、烧牌、52 张牌守恒和现金桌补充记账。
- 随机模拟：10,000 手，33,118 个边池，2,068 次平分，单手最多 8 个池；无重复牌、非法获奖或筹码异常。
- AI 可重复基准：初/中/高模拟次数分别为 35/70/210，判断噪声为 0.22/0.10/0.008，开池加注基线为 0.52/0.76/0.98；行为场景分离断言通过。
- 严格教练审计：4,179/4,179 检查通过，包含 324 个交叉稳定性场景和 3,672 个不变量模糊检查。
- Chrome 响应式布局：804×305 的 6 人短宽屏无座位/中央区重叠；半屏竖向布局通过。
- Chrome 端到端：设置 6 人现金桌、全押、摊牌、最佳五张、手间补充、下一手和历史落盘通过；`console error = 0`，`pageerror = 0`，HTTP 4xx/5xx = 0。
- Codex 应用内真实浏览器：手动完成“设置→入场→全押→六人摊牌→最佳五张→玩家破产补充→下一手→回放”。

## 2026-09-01 发布前复验

再次执行 `./test-local.command`，规则回归、10,000 手随机模拟、三档 AI 基准、范围/策略包/求解路由、桌面场景、4,179/4,179 严格教练审计、Chrome 响应式布局和完整牌局端到端测试全部通过。端到端测试的浏览器控制台错误、页面错误与 HTTP 4xx/5xx 均为 0。

## 已知边界

- 本地翻牌后 WASM 求解器只覆盖声明的单挑抽象节点。多人底池、全押节点、无可信范围或不受支持的浏览器会使用明确标注的范围/胜率启发式建议，不会冒充精确 GTO。
- 高级 AI 是强化的本地策略模型，不等于无抽象、无误差的 GTO 代理。
